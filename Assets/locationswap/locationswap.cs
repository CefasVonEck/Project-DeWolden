using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// Scenemanager

public class locationswap : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // during collision
        // show pop up
            // if clicked yes move to location
                // move the player and side character out of the scene 
                    // wait till they are out of the scene and show a black overlay
                        // change the scene
}
